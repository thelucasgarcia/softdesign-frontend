import { DefaultTheme } from 'styled-components'

const theme = {
    colors: {
        primary: "#EC1D24",
        text: "#FFF",
        light: '#FAfAFA',
        dark: "#202020",
        background: '#121212',
    }
} as const;

export default theme