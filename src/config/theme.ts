import { DefaultTheme } from 'styled-components'

const theme = {
    colors: {
        primary: "#EC1D24",
        text: "#FFF",
        light: '#FAfAFA',
        background: "#202020",
    }
} as const;

export default theme